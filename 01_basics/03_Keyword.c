#include<stdio.h>

int main(){

    //'int' is a keyword used to declare an integer variable
    int age = 20;

    //'float' is a keyword used to declare a floating-point variable
    float height = 5.5;

    //'char' is a keyword used to declare a character variable
    char grade = 'A';

    // 'if' and 'else' are keywords used for decision making
    if (age>=18){
        printf("You are an adult.\n");
    } else {
        printf("You are minor.\n");
    }

    // 'return' is a keyword used to return a value from a function
    return 0;
}